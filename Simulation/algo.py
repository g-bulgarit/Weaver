def select_next_peg(image):
    # from current_peg, get values of the row of pixels between this peg
    # and all others, find out which peg results in the highest value,
    # select that peg to be the next peg.
    pass